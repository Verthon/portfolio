dist
node_modules
pnpm-lock.yaml
.astro
.netlify
playwright-report
test-results
tsconfig.tsbuildinfo
**/*.log
**/.DS_Store

# MDX is never formatted. Do not remove this, and do not run Prettier on an
# .mdx file by hand — its markdown parser re-indents closing JSX tags after
# lists, producing invalid MDX that breaks the build (prettier#16925).
# Fixed once in ca8766d; it will come back the same way.
**/*.mdx
